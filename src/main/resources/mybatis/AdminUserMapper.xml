<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.admin.user.store.MyBatisAdminUserStore">
    <select id="findById" resultType="com.example.admin.user.store.MyBatisAdminUserStore$UserRow">
        SELECT USER_ID AS id,
               LOGIN_ID AS loginId,
               USER_NAME AS name,
               ENABLED AS enabled,
               CREATED_BY AS createdBy,
               CREATED_AT AS createdAt,
               UPDATED_BY AS updatedBy,
               UPDATED_AT AS updatedAt
          FROM ADMIN_USER
         WHERE USER_ID = #{id}
    </select>

    <select id="search" resultType="com.example.admin.user.store.MyBatisAdminUserStore$UserRow">
        SELECT USER_ID AS id,
               LOGIN_ID AS loginId,
               USER_NAME AS name,
               ENABLED AS enabled,
               CREATED_BY AS createdBy,
               CREATED_AT AS createdAt,
               UPDATED_BY AS updatedBy,
               UPDATED_AT AS updatedAt
          FROM ADMIN_USER
         <where>
             <if test="loginKeyword != null and loginKeyword != ''">
                 UPPER(LOGIN_ID) LIKE '%' || UPPER(#{loginKeyword}) || '%'
             </if>
         </where>
         ORDER BY USER_ID
    </select>
</mapper>
