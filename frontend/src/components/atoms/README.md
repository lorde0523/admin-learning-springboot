# Atoms

Atoms are indivisible presentation controls such as `Button`, `TextInput`, `Badge`, and `Spinner`.

- Accept values, visual variants, and DOM callbacks through props.
- Do not fetch data, know routes, or import React Query.
- Preserve native semantics and accessibility attributes.
- Place a component here when it is useful without another project component.

