# Molecules

Molecules combine atoms into one small interaction, such as `SearchField`, `FormField`, `StatusMessage`, or `GridToolbar`.

- Own only local interaction needed by that composition.
- Do not call API or query hooks.
- Import atoms, never organisms, templates, or pages.
- Place a component here when two or more atoms form one named control.

