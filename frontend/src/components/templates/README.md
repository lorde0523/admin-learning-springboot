# Templates

Templates define page layout without owning domain requests. Examples are `WorkbenchTemplate` and `ListDetailTemplate`.

- Arrange organisms and shared page regions.
- Render slots or children supplied by pages.
- Do not call APIs or React Query hooks.
- Place a component here when its primary responsibility is reusable page composition.

