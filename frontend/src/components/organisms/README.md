# Organisms

Organisms are substantial UI regions such as `UserEditor`, `RoleAssignmentPanel`, `MenuGrid`, and `SqlTracePanel`.

- May contain domain-specific display and editing behavior.
- Receive remote data and mutation callbacks from pages.
- May import atoms and molecules.
- Do not import pages or perform Axios requests.

